name: Release

on:
  push:
    branches: [main]

jobs:
  release-please:
    name: Release Please
    permissions:
      contents: write # Required to update changelog file
      pull-requests: write # Required to create release PRs
    uses: equinor/ops-actions/.github/workflows/release-please-manifest.yml@8dfda59d14ec91a8727ac64fe60ab51a4b98d03e # v9.35.0
